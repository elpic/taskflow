---
name: tasks
description: "Show the current taskflow task tree"
---

Call the `task_list` tool to display all tasks as a visual tree. If there is a current task, also call `task_current` to highlight the active work.
