---
name: version
description: "Show the current taskflow version and tool count"
---

Run `taskflow-mcp --version` to get the current binary version. Also count the registered MCP tools by calling `task_types` or inspecting the server. Report concisely:

- Taskflow version
- Number of MCP tools
- Number of workflow types
